package geometriaCalc; // define o pacote onde a interface está localizada

// interface que representa uma forma geométrica
public interface GeometricForm {

    // método que deverá ser implementado para calcular a área da forma
    double getArea();
}
